const arr = [1, 5, 11, 5]
let n = arr.length

function equal(arr, n) {
    let sum = 0
    // plus the whole arr and stor in sum
    for (let i = 0; o < n; i++)   sum += arr[i]
    if (sum % 2 !== 0) return false
    else if (sum % 2 == 0) {

    }
}
